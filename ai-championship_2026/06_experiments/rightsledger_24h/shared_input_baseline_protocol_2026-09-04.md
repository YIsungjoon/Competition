# 범용 LLM 공통입력 기준선 재실행 프로토콜

- 고정일: 2026-09-04, Asia/Seoul
- 상태: 실행 전 고정
- 실험 ID: `EXP-RL-20260904-03`
- 목적: 범용 LLM과 RightsLedger 하이브리드의 입력 정보량을 같게 만들어 기존 기준선의 입력 비대칭 영향을 확인한다.

## 발견한 교란요인

기존 범용 LLM 실행은 프로젝트 파일 15개의 경로·추출 텍스트만 받았다. 하이브리드는 여기에 더해 `.rightsledger_observations.json`의 자산 경로, 중립 설명, 취득·생성 유형을 받았다. 따라서 기존 `0.25` 누락 재현율을 하이브리드와 직접 비교하면 입력 정보량 차이가 모델 차이로 섞인다.

## 고정 입력

두 조건 모두 다음을 받는 것으로 정의한다.

1. 프로젝트 파일의 경로와 추출 가능한 원문
2. 동일한 공통 자산 선언: `path`, `description`, `origin_type`

공통 자산 선언에는 정답 관계, 고의 누락 목록, 임베딩 점수, 권리 결론을 넣지 않는다. 현재 합성 사례에서는 기존 `.rightsledger_observations.json`을 그대로 사용한다.

## 고정 실행 조건

- 프롬프트 본문: `general_llm_baseline_prompt_v1.md`의 마커 내부 문구, 변경 없음
- 모델: 로컬 `qwen3:4b`, ID `359d7dd4bcda`
- temperature: `0`
- seed: `20260904`
- num_ctx: `32768`
- thinking 요청: `false`
- 사례당 실행: 1회
- 부분 실패 시: 이미 저장된 사례의 입력·출력 해시가 같으면 재호출하지 않고 이어서 실행
- 원시 응답: 수정 없이 사례별 JSON과 JSONL로 보존
- 출력 정리: 기존 고정 규칙대로 마지막 `</think>` 뒤를 체크리스트로 사용

## 해석 제한

이 재실행도 동일 분석자가 만든 합성 데이터이므로 정식 블라인드 점수가 아니다. 목적은 기존 범용 LLM 성능을 방어하거나 RightsLedger를 유리하게 만드는 것이 아니라, 독립 검증 전에 비교 입력을 대칭으로 고치는 것이다.

공통 선언은 실제 미디어 이해 성능을 대신한다. 따라서 이 실험이 통과해도 이미지·음원 내용 추출과 자산 유형 확인은 별도 게이트로 남긴다.
