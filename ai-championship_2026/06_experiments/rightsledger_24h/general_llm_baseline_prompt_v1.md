# 범용 LLM 체크리스트 기준선 프롬프트 v1

- 고정일: 2026-09-04, Asia/Seoul
- 상태: 2026-09-04 합성 사례 5건 실행 완료, 프롬프트 본문 변경 없음
- 목적: RightsLedger 전용 관계 그래프나 임베딩 없이, 같은 프로젝트 자료를 범용 LLM에 주었을 때 얻는 감사 체크리스트를 측정한다.

## 프롬프트

<!-- PROMPT_START -->
아래에는 한 프로젝트 폴더의 파일 경로와 각 파일에서 읽을 수 있는 내용이 제공됩니다. 이 자료만 사용해 제출 또는 납품 전에 사람이 확인해야 할 출처·라이선스·구매증빙·AI 생성 기록 체크리스트를 작성하세요.

각 결과물에 사용된 것으로 보이는 입력 자산을 연결하고, 각 입력 자산에 현재 존재하는 증빙과 빠진 것으로 보이는 증빙을 나누어 적으세요. 연결 근거가 약하면 추측으로 확정하지 말고 `확인 필요`라고 표시하세요. `권리가 있다`, `침해다`, `법적으로 사용 가능하다` 같은 법률 결론은 내리지 마세요.

형식은 사람이 읽는 Markdown 체크리스트로 자유롭게 작성하되, 판단마다 근거가 된 파일 경로를 적으세요.
<!-- PROMPT_END -->

## 고정 규칙

- 모든 조건에서 위 문구를 그대로 사용한다.
- 모델명·버전, 온도, 컨텍스트 제한, 실행 시각을 결과 옆에 기록한다.
- 한 사례의 답을 본 뒤 프롬프트를 고치지 않는다.
- 폴더 원문 외에 정답표, 임베딩 점수, RightsLedger 예측은 모델에 주지 않는다.
- 모델 출력은 사람이 검토 완료할 때까지 수정하지 않고 원본으로 보존한다.

## 고정 실행 조건

- 모델: 로컬 `qwen3:4b`, ID `359d7dd4bcda`
- temperature: `0`
- seed: `20260904`
- num_ctx: `32768`
- thinking: `false`
- 출력 정리: 로컬 API가 `thinking:false`에도 본문에 사고문과 `</think>`를 섞는 현상이 비데이터 점검 2회에서 재현됐다. 원시 응답은 보존하고 마지막 `</think>` 뒤만 체크리스트로 분리한다.
- 파일 전달 형식: 숨김 관찰 파일을 제외한 `경로 + 추출 텍스트` 단일 묶음
- 실행 횟수: 합성 사례당 1회, 총 5회
- 실제 모델 추론시간: 합계 `936.624초`; 사람 검토시간이 아님
