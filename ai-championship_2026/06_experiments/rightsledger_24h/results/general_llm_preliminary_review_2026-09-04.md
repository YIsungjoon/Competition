# 범용 LLM 체크리스트 예비 검토

> 2026-09-07 후속 점검에서 하이브리드만 자산 설명·취득 유형을 받은 입력 비대칭을 확인했다. 이 문서의 출력은 보존하되 직접 비교 근거로 사용하지 않는다. 동일 공통입력 재실행 결과는 `general_llm_shared_preliminary_review_2026-09-07.md`에 있다.

- 실행일: 2026-09-04, Asia/Seoul
- 모델: 로컬 `qwen3:4b`, ID `359d7dd4bcda`
- 사례: 기존 합성 프로젝트 5개, 사례당 1회
- 상태: **비블라인드 사후 분석자 코딩**. 정식 정확도나 사람 시간 결과가 아니다.
- 원시 응답: `general_llm_raw.jsonl`
- 분리 체크리스트: `general_llm_outputs/`
- 누락 코딩 원문: `general_llm_preliminary_missing_coding_2026-09-04.jsonl`

## OBSERVATION — 실행 무결성

- 다섯 요청은 같은 프롬프트 해시 `b922befad4ca1cefe4b72c2f062c6168db5ed010659bfaecb042434fa774040b`를 사용했다.
- 각 요청에는 감사 대상 파일 15개의 경로와 내용만 포함됐다. 정답표, `.rightsledger_observations.json`, 임베딩 점수는 제외했다.
- temperature 0, seed 20260904, num_ctx 32768, thinking false로 실행했다.
- 다섯 응답 모두 사고문이 `message.content`에 섞여 마지막 `</think>` 뒤를 체크리스트로 분리했다. 원시 응답은 수정 없이 보존했다.
- 모델 추론시간 합계는 936.624초, 약 15분 37초다. 이는 사람의 검토시간이 아니다.

## OBSERVATION — 명시적 누락 탐지

정답상 누락 증거는 총 4건이다.

| 사례 | 정답 누락 | 범용 LLM 체크리스트 |
|---|---|---|
| CASE-002 | AI 음성의 `model_terms` | 탐지 |
| CASE-003 | 미사용 기타 음원의 `receipt` | 미탐지 |
| CASE-003 | 미사용 기타 음원의 `license` | 미탐지 |
| CASE-005 | 폰트의 `source` | 미탐지 |

직접 대응 가능한 문구만 보수적으로 코딩하면 명시적 누락 재현율은 `1/4 = 0.25`다. 생성 음원에 별도 라이선스가 없다는 경고 1건은 현재 실험 규칙상 명확한 과잉 경고다. CASE-001의 이용조건 재확인, CASE-002의 저작권자 정보, CASE-004의 AI 기록 확인 경고는 현재 정답 스키마와 직접 대응되지 않아 정밀도 계산에서 제외했다.

이 0.25는 출력 후 분석자가 정답을 보며 코딩한 예비값이다. 사전에 정한 독립 평가자의 블라인드 점수로 사용하지 않는다.

## OBSERVATION — 근거와 안전 문제

- CASE-005는 존재하지 않는 `evidence/css_authorship.txt`를 인용했다. 실제 파일은 `evidence/author/css_authorship.txt`다.
- CASE-005는 폰트 원출처가 없는데도 `No missing evidence`, `All evidence is present`, `Submit project with confidence`라고 결론 내렸다.
- 같은 답변에서 `Status: Licensed`, `Team-owned`, `No external licenses are required`라고 단정했다. 이는 프롬프트가 금지한 권리·제출 가능성 판단에 해당할 위험이 높다.
- CASE-003은 사용되지 않은 대체 기타 음원도 프로젝트 감사 대상이라는 규칙을 놓치고 누락이 없다고 표시했다.

## INTERPRETATION

범용 LLM은 문서에 드러난 쉬운 연결과 CASE-002의 모델 약관 누락은 찾았다. 그러나 `미사용이지만 폴더에 남은 자산`, `라이선스는 있지만 원출처가 없는 자산`, `존재하는 경로의 정확성`, `법률·제출 확신 억제`에서 실패했다.

이는 RightsLedger의 차별점을 더 선명하게 만든다. AI가 자유롭게 최종 체크리스트를 쓰게 하는 것이 아니라, AI는 관계 후보만 만들고 모든 경로·필수 증거·출력 문구는 결정론적으로 검증해야 한다.

## 결정에 미친 영향

- 범용 LLM 기준선 출력 5건 확보는 완료했다.
- 하이브리드의 우위를 정식 확정하지 않는다. 관계 F1 코딩과 실제 검토 완료시간은 독립 검토자에게 남긴다.
- 다음 블라인드 평가에서는 존재하지 않는 경로 인용 수와 금지 결론 수도 보조 안전지표로 추가한다.

## 한계와 다음 확인

- 4B 로컬 모델 하나의 결과이며 상용 대형 모델 일반화 근거가 아니다.
- 현재 데이터는 분석자가 만든 합성 사례다.
- 응답의 사고문이 비정상적으로 길어 추론시간이 커졌지만, 사람 검토시간과 제품 응답시간 게이트는 아직 정하지 않았다.
- 당시 다음 단계는 `blind_validation_protocol_2026-09-04.md` 실행이었다. 입력 비대칭을 확인한 뒤 현행 절차는 `blind_validation_protocol_v2_2026-09-07.md`로 대체됐다.
